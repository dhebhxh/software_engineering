class PhysicsSystem {
    constructor(entities) {
        this.entites = entities;
        this.dynamicObjects = [];
    }
    filter(){
        //this.entities -> this.dynamicObjects;
    }
    update() {
        for(const dynamic of this.dynamicObjects) {
            
        }
    }
}