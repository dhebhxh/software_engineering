class UIManager {
    constructor() {

    }
    //
    changeUI(resultMsg) {
        //asdfasdfasd
    }
    detectClickOnLevel() {
        const levelIndex = whichLevel();
        this.levelManager.loadLevel(levelIndex);
    }
}